# test-cmd

The test-cmd command is used for testing purposes.

## Usage

Run the command with optional arguments to test the system.

## Options

- `--verbose` — Enable verbose output
- `--dry-run` — Show what would happen without making changes
